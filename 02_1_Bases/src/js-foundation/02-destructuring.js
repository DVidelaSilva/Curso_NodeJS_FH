//console.log( process.env )

const { SHELL, VSCODE_INJECTION } = process.env;

//console.log(SHELL, VSCODE_INJECTION)

//console.table({SHELL, VSCODE_INJECTION})

const characters = ['Flash', 'Superman', 'Green Lanter', 'Batman'];

const [, , ,Batman] = characters;

